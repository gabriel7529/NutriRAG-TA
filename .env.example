# Archivo de ejemplo para configurar variables de entorno
# Copia este archivo como .env y completa con tus valores reales

# Configuración de OpenAI
# Obtén tu API key en: https://platform.openai.com/api-keys
OPENAI_API_KEY=api_key_here
# Modelo de OpenAI a usar
# Opciones disponibles: gpt-4, gpt-4-turbo, gpt-3.5-turbo, gpt-4o-mini
OPENAI_MODEL=gpt-4o-mini

# Máximo número de tokens en la respuesta (100-4000)
OPENAI_MAX_TOKENS=400

# Temperatura para la generación (0.0-1.0)
# 0.0 = respuestas muy predecibles
# 1.0 = respuestas muy creativas
OPENAI_TEMPERATURE=0.7

# Configuración del modelo de embeddings
EMBEDDING_MODEL=all-MiniLM-L6-v2

# Configuración de desarrollo
DEBUG=True
LOG_LEVEL=INFO

# Base de datos (opcional)
DATABASE_URL=sqlite:///./data/nutrition.db

# Configuración de CORS para frontend
CORS_ORIGINS=["http://localhost:3000", "http://localhost:8080"]
